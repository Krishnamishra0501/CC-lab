%{
#include <stdio.h>
int count = 0;
%}

%%

.*COM[ \t]*\n    { count++; }
.*COM[ \t]*      { count++; }
[^\n]*\n         ;
.                ;

%%

int yywrap(void)
{
    return 1;
}

int main()
{
    yylex();
    printf("Lines ending with COM: %d\n", count);
    return 0;
}
